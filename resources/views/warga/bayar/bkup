<table class="table table-hover">
            <thead>
              <tr>
                <th>No</th>
                <th>Judul</th>
                <th>Nama Iuran</th>
                <th>Nominal</th>
                <th>Tanggal</th>
                <th>Informasi</th>
                <th>Status</th>
                <th>menu</th>
              </tr>
            </thead>
            <tbody>
              @if($notifiuran->count() > 0)
              @foreach($notifiuran as $rs)
              <tr>
                <td>{{$loop->iteration }}</td>
                <td>{{$rs->judul}}</td>
                <td>{{$rs->iuran->nama_iuran}}</td>
                <td>Rp. {{$rs->nominal}}</td>
                <td>{{$rs->tanggal}}</td>
                <td>{{$rs->informasi ?? '-'}}</td>
                <td>{{$rs}}</td>
                <td><label class="badge badge-danger"><a class="btn btn-rounded btn-primary" href="{{route('warga.bayariuran', $rs->id_notifikasi)}}" type="button">Bayar</a></label> 
                  {{--<label for="badge badge-danger">
                    <form action="{{route('deleteIuran', $rs->id_iuran)}}" method="POST" onsubmit="return confirm('Delete?')">
                      @csrf
                      @method('DELETE')
                      <button>Delete</button>
                    </form>
                  </label>
                </td> --}}
              </tr>
              @endforeach
              @else
              <tr>
                <td>
                  Product Not Found
                </td>
              </tr>
              @endif
            </tbody>
          </table>